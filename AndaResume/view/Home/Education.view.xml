<mvc:View
  controllerName="chuntian.resume.controller.Home.Education"
  xmlns="sap.m"
  xmlns:c="sap.ui.core"
	xmlns:t="sap.ui.table"
	xmlns:f="sap.ui.layout.form"
  xmlns:mvc="sap.ui.core.mvc"
  displayBlock="true">
  
  <Panel headerText="{i18n>Education_hdtxt}" id ="SAPUI5" class="sapUiResponsiveMargin" width="auto">
    <headerToolbar>
      <Toolbar height="3rem">
        <Button text="{i18n>Education_hdtxt}" icon="sap-icon://study-leave" press="onPress1" />
      </Toolbar>
    </headerToolbar>    
    <content>
			<Label text="{i18n>Education_Sctxt}" />
			<Text text="{i18n>Education_Schol}" />            
			<Label text="{i18n>Education_Titxt}" />
			<Text text="{i18n>Education_Timel}" />
			<Label text="{i18n>Education_Mjtxt}" />
			<Text text="{i18n>Education_Major}" />         
    </content>
  </Panel>
</mvc:View>